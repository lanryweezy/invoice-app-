{
  "name": "invoiceapp-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter \"./apps/**\" --parallel run dev",
    "build": "pnpm --filter \"./apps/**\" run build",
    "test": "pnpm --filter \"./apps/**\" run test"
  }
}
